Declaration:

unordered_map<int,int> mp
{
    {1,10},{2,5},{3,10},{4,50}
};


// Access elements

for(auto [key, val]: mp) {....}

for(auto it: mp) 
{
    auto key = it.first;
    auto val = it.second;
}
